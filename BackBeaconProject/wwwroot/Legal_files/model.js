/**
 * Base model for all models within Adx.
 **/
define(function (require) {
    var Backbone = require('backbone'),
        Model;

    Model = Backbone.Model.extend({

    });

    return Model;
});
